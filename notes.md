# tailwind instalar y crear archivo tailwind y postcss
# Alias configurar en tsconfig y vite.config
# React Router Dom. Agregado en main.tsx y App.tsx

# useContext
Se crea el archivo de contexto con las funciones globales que queremos usar para setear o quitar productos  y el setState
se engloba la aplicacion App.tsx con el provider
luego se importa y se usan las funcioens de add y delete
con useContext se toman los valores